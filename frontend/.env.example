# Frontend environment example
# Copy this file to .env (NOT committed if .gitignore configured) and adjust values.

# Base HTTP API for FastAPI backend (no trailing slash)
VITE_API_URL=http://localhost:8000

# Base WebSocket URL without trailing room id, the UI appends /{room_id}
# Use ws:// for local dev, wss:// for production with TLS
VITE_WS_URL_BASE=ws://localhost:8000/ws
